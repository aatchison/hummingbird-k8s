# Generated at runtime / per-host secrets
bib-config.toml
bib-config-*.toml
worker-join.env
admin.conf
admin.conf.*

# Operator-fetched kubeconfigs (full-cluster credentials — never commit)
/kubeconfig.yaml
/argocd-kubeconfig.yaml

# Logs from build/redo scripts
*.log

# Local kubectl wrapper cache (server URLs vary)
/tmp/k8s-kubeconfig

# Per-host config overrides
config.local.sh
cluster.local.conf

# OS/editor noise
.DS_Store
*.swp
.vscode/
